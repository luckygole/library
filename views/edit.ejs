<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create new item</title>
</head>
<body>
    <h2>create new listening</h2>
  
        <input type="number" name="year" placeholder="enter year"><br><br>
        <input type="text" name="semester" placeholder="enter semester"><br><br>
        <input type="text" name="subject" placeholder="enter subject"><br><br>
        <textarea name="content" placeholder="enter content" rows="15" cols="50"></textarea><br><br>
        <button>Add your content</button>
    </form>
</body>
</html> -->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Form</title>
    <style>
        body {
            background: linear-gradient(to right, #ffffff, #f0f0f0);
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .form-container {
            background: rgba(255, 255, 255, 0.7);
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            width: 100%;
            max-width: 500px;
        }
        .form-container h2 {
            text-align: center;
            margin-bottom: 20px;
        }
        .form-container input, 
        .form-container textarea {
            width: 95%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 8px;
            box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
            font-size: 16px;
        }
        .form-container textarea {
            resize: vertical;
            height: 100px;
        }
        .form-container button {
            width: 100%;
            padding: 10px;
            background-color: #000;
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .form-container button:hover {
            background-color: #444;
        }

        @media (max-width: 768px) {
            .form-container {
                padding: 15px;
            }
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>Edit the content</h2>
        <form method="post" action="/home/<%= syllabus._id%>/?_method=PUT"">
            <input type="number" name="syllabu[year]" value="<%= syllabus.year %>" placeholder="enter year"><br><br>
            <input type="text" name="syllabu[semester]" value="<%= syllabus.semester %>" placeholder="enter semester"><br><br>
            <input type="text" name="syllabu[subject]" value="<%= syllabus.subject %>" placeholder="enter subject"><br><br>
            <textarea name="syllabu[content]"  placeholder="enter content"><%= syllabus.content %></textarea><br><br>
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
