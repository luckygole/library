<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Note to <%= subject.name %></title>
</head>
<style>
  body {
      background: linear-gradient(to right, #ffffff, #f0f0f0);
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
  }
  
h1, h2, h3 {
    margin: 0;
}
a {
    text-decoration: none;
    color: #000;
}

/* Navbar Styles */
nav {
    background: #000;
    padding: 15px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    position: relative;
    overflow: hidden;

}
nav ul {
    list-style: none;
    display: flex;
    gap: 20px;
    transition: all 0.3s ease-in-out;
}
nav ul li {
    display: inline;
}
nav ul li a {
    color: #fff;
    font-size: 18px;
    transition: color 0.3s;
}
nav ul li a:hover {
    color: #ff5722;
}

/* Navbar for Mobile */
.menu-btn {
    display: none;
    cursor: pointer;
    font-size: 28px;
    color: #fff;
}

.nav-links {
    display: flex;
}

  .form-container {
      background: rgba(255, 255, 255, 0.7);
      border-radius: 15px;
      padding: 20px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(10px);
      width: 100%;
      max-width: 500px;
  }
  .form-container h2 {
      text-align: center;
      margin-bottom: 20px;
  }
  .form-container input, 
  .form-container textarea {
      width: 95%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 8px;
      box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
      font-size: 16px;
  }
  .form-container textarea {
      resize: vertical;
      height: 100px;
  }
  .form-container button {
      width: 100%;
      padding: 10px;
      background-color: #000;
      color: #fff;
      border: none;
      border-radius: 8px;
      font-size: 18px;
      cursor: pointer;
      transition: background-color 0.3s ease;
  }
  .form-container button:hover {
      background-color: #444;
  }
  .back-btn{
    text-decoration: none;
    position: relative;
    margin-top: 20px;
    padding-top: 10px;
    margin-left: 70%;
    color: #000 !important;
  }

  @media (max-width: 768px) {
      .form-container {
          padding: 15px;
      }
  }
</style>
<body>

   

  <div class="form-container">
    <h1>Add Note to <%= subject.name %></h1>

    <form action="/subject/<%= subject._id %>/add-note" method="POST">
     
      <label>Heading:</label>
      <input type="text" name="heading" required>
      
      <label>Subheading:</label>
      <input type="text" name="subheading" required>
      
      <label>Content:</label>
      <textarea name="content" required></textarea>
      
      <label>Image URL (optional):</label>
      <input type="text" name="image">
      
      <button type="submit">Add Note</button>
    </form>

    <a href="/subject/<%= subject._id %>" class="back-btn">Back to Subject</a>
    </div>
</body>
</html>